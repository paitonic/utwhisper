
utwhisper -- control your utserver (utorrent for linux) using console.

Tested with Python 2.7.2.

Help:		
python utwhisper.py --help
see doc/ for index.html too.

Notes:
Your utserver must be running.
Edit settings.py (server, user, passwd).
